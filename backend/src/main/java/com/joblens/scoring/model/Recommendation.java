package com.joblens.scoring.model;

/** What to do about this posting. Never a prediction about being hired. */
public enum Recommendation {
    STRONG_APPLY,
    APPLY,
    CONDITIONAL,
    LOW_PRIORITY
}
